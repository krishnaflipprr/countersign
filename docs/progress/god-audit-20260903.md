<!-- audited on 20260903 -->
# God Audit: Countersign, 2026-09-03

Serial, one file at a time, bottom-up dependency order, every line read.
A file is stamped `audited on 20260903` only after it is 100%: no stubs, no
placeholders, no false statements, real data flow end to end, and it runs.
Where a file was not 100%, the code was fixed to green first and the fix is
recorded here. Tests were written before every behaviour change to a check.

## Standing verification (after the audit)

| Check | Result |
| --- | --- |
| `python3 -m unittest discover -s tests -t .` | 61 tests, OK (was 27) |
| `python3 -m countersign verify` | COUNTERSIGNED, exit 0, 11 files, 5 exemptions used, 2 inert markers noted |
| `python3 -m countersign verify --config demo/countersign.toml` | NOT COUNTERSIGNED, exit 1, 4 findings, 1 failed claim (as designed) |
| `python3 -m countersign check` | chain intact |
| `pip wheel .` then install into a clean venv | `countersign 0.1.0` boots; `init` + `verify` on a fresh tree countersigns |
| Evidence pack HTML (pass and fail variants) | rendered in a headless browser, both correct |
| Argus `scripts/check-no-stubs.sh` (source of the rules) | exit 0 on its 562-file tree, so the zero-false-positive statement still holds |

## Ledger

| # | File | Status | What was found and what was done |
| --- | --- | --- | --- |
| 1 | `.gitignore` | stamped (no stamp, not a comment-bearing file) | Correct; `.countersign/` ignored so receipts never land in git. |
| 2 | `pyproject.toml` | stamped | Added the 3.14 classifier (the suite runs on 3.14). Wheel builds and installs. |
| 3 | `countersign/__init__.py` | stamped | Version matches pyproject. |
| 4 | `countersign/__main__.py` | stamped | Correct. |
| 5 | `countersign/config.py` | stamped | **Fixed:** a scan path that does not exist was silently skipped, so a typo in `paths` produced a scan of nothing that then passed. Now a `ConfigError`. **Fixed:** a path outside the root crashed with a traceback; now a `ConfigError`. **Fixed:** malformed TOML and wrong-typed values (string where a list is needed, `timeout_s = 0`, booleans as integers) raised raw Python errors; all are now `ConfigError` with the reason. `claims.file = ""` now cleanly disables claims. |
| 6 | `countersign/stubscan.py` | stamped | **Fixed (false positives):** methods of a generic `Protocol[T]` were reported as empty bodies; a UTF-8 BOM made a valid file "unparseable". **Fixed (wrong line numbers):** `str.splitlines()` splits on form feeds and Unicode separators that Python's tokenizer does not, so every finding after a form feed was off by one and the empty-body exemption check read the wrong line. Lines are now split on `\r\n`, `\r`, `\n` only. **Fixed (crash on 3.11):** a null byte raises `ValueError` on 3.11 (SyntaxError on 3.12+); now caught either way. **Fixed:** dead decorator names `overridable` and `abc.override` (the latter could never match) removed. **Fixed (wrong count):** exemptions were counted for every line carrying the marker, including the marker's own definition, and a def-line marker was counted twice. Now only a marker that suppressed a finding counts as used; markers that suppress nothing are reported separately as inert. Six of the eleven rule-table markers were inert (a regex with alternation does not match its own source line) and were removed; the five that matter stay and are pinned by a claim. |
| 7 | `countersign/register.py` | stamped | **Fixed (chain could be broken by a race):** two runs on one checkout could read the same head and both append the same index, leaving a register broken forever and indistinguishable from tampering. Appends now hold an exclusive lock (`register.jsonl.lock`, flock on POSIX, msvcrt on Windows) across read-head and write. Verified with 48 concurrent appends from 8 threads. **Fixed:** binary garbage in the file raised `UnicodeDecodeError` instead of a verdict; a last line that parsed as JSON but was not an entry (a bare number) escaped `RegisterDamaged`. **Fixed:** constructing a `Register` created the receipts directory even for read-only commands. |
| 8 | `countersign/claims.py` | stamped | **Fixed (crash):** a command that printed anything and then timed out crashed the whole run (`TypeError: can't concat str to bytes`, because `TimeoutExpired` carries bytes even in text mode). **Fixed (crash):** any command output that was not valid UTF-8 raised `UnicodeDecodeError`. **Fixed (leak):** a timed-out command's children survived the kill; commands now run in their own process group and the whole tree is killed. **Fixed:** malformed claims TOML, `[claim]` written as a table instead of `[[claim]]`, and non-integer or zero `timeout_s` raised raw errors; all are `ClaimsError` now. Removed `run_claims`, which became unused. |
| 9 | `countersign/engine.py` | stamped | **Fixed (docstring was false):** it said the run is recorded before checks run; in fact scan and claims ran first and nothing reached the register until the end, so a killed run left no trace. `run_started` is now on disk before any check runs, findings and claim verdicts are appended as they happen, and a run that dies appends `run_aborted` on the way out. **Fixed (misleading receipt):** receipts named a commit with no indication the working tree differed from it. Receipts now record `git_dirty` and print "(uncommitted changes in the working tree)". `git not available` and `not a git repository` are now distinguished. **Fixed:** `--no-claims` skipped claims with no note on the receipt. **Fixed:** a missing config file crashed with `FileNotFoundError`; now a `ConfigError`. A bad config or claims file now refuses the run before anything is written. |
| 10 | `countersign/receipt.py` | stamped | **Fixed:** evidence containing a backtick (TypeScript template literals) or a multi-line command broke the Markdown tables; code spans now use double-backtick delimiters and flatten whitespace. Receipt JSON gains `git_dirty` and `scan.tests_excluded`. Skipped claims are now labelled "skipped, not passed". |
| 11 | `countersign/pack.py` | stamped | **Fixed (false statement):** the "not covered" list said test files were excluded even when `exclude_tests = false`; the bullet is now conditional. **Fixed (overstatement):** "every check above is deterministic" covered claim commands, which are the team's own; wording now separates the deterministic scan from claims run as declared. **Fixed (overstatement):** the footer said "no data left the machine", which Countersign cannot promise about claim commands; it now says Countersign itself made no network requests. Full SHA-256 shown instead of a truncated one with a literal "..." after "none declared". Added the `unparseable` row to the method table. |
| 12 | `countersign/reproduce.py` | stamped | **Fixed:** a hand-edited or truncated receipt crashed reproduce with `KeyError` or `JSONDecodeError`; every malformed shape is now a "not reproduced" verdict with the reason. **Fixed:** a claims file that no longer parses crashed instead of being reported. Removed `_claim_key`, dead code. Added a note when the number of scanned files differs from the run. |
| 13 | `countersign/cli.py` | stamped | **Fixed:** `check` caught `RegisterDamaged`, which `verify_chain` never raises, and nothing caught `OSError`. Config and claims errors now exit 2 (usage) instead of 1. Interrupt exits 130, not 2 (interrupt is not a usage error). The `init` template now matches the defaults (`.hg`, `.svn`) and says `max_output_bytes` is in characters, which is what the code measures. The register is verified once and reused rather than opened twice. |
| 14 | `countersign.toml` | stamped | Aligned `ignore_dirs` with the defaults. |
| 15 | `claims.toml` | stamped | The self-scan claim now also pins the used-exemption count at five. Added `register-chain-intact` (`countersign check`). |
| 16 | `tests/__init__.py` | stamped | Correct. |
| 17 | `tests/tamper_check.py` | stamped | Correct; fail-safe if the register format changes (an ineffective edit leaves the chain intact and the script exits 1). |
| 18 | `tests/test_config.py` | new, stamped | Covers every ConfigError above and the collect_files filters. |
| 19 | `tests/test_stubscan.py` | stamped | Added form feed, generic Protocol, abstractmethod, BOM, null byte, single-count and inert-marker tests. |
| 20 | `tests/test_register.py` | stamped | Added concurrency, binary garbage, and read-does-not-create-directory tests. |
| 21 | `tests/test_claims.py` | stamped | Added output-before-timeout, undecodable output, process-tree kill, malformed TOML, table-vs-array and bad-timeout tests. |
| 22 | `tests/test_gate.py` | stamped | Added run-start-before-claims (the claim command reads the register), missing config, claims-off note, conditional pack bullet, backtick escaping, git dirty state (real git repo), inert marker note, reproduce file-count note, damaged receipt tests. |
| 23 | `demo/countersign.toml` | stamped | Correct. |
| 24 | `demo/claims.toml` | stamped | **Fixed (wrong disproof command):** `wc -l \| grep -qx 0` can never match on macOS because `wc` pads its output, so the claim failed even on a clean tree. Now `grep -rqE` with `expect = "nonzero exit"`, portable. |
| 25 | `demo/app.py` | stamped | The planted defects are exactly the four the README promises; the file is a fixture and is meant to fail its gate. |
| 26 | `demo/test_app.py` | stamped | Correct. |
| 27 | `action.yml` | stamped | **Fixed:** `pip install` of the action path fetched setuptools from PyPI at run time, contradicting "zero dependencies"; the action now runs the checkout directly via `PYTHONPATH`. Inputs are passed through `env` rather than interpolated into the shell. Added `receipts-dir` so the artifact upload follows a moved receipts directory. Python pinned to 3.12 (3.11 is the floor, not the target). Branding icon `verified` is not a valid GitHub action icon; now `check-circle`. |
| 28 | `README.md` | stamped | **Fixed (impossible example):** the sample output showed an `empty-body` finding on a `.ts` file; that check is Python only. **Fixed (broken example):** the `pricing-is-live` claim piped through `grep -q`, which prints nothing, so `output contains` could never match. Sample output now matches the real terminal format (full commit, milliseconds, the policy note). Removed "documented" from the false-completion sentence (no source was cited) and "bank-grade" from the roadmap (nothing here has been through a bank). "production-certified" replaced by "reviewed file by file", per the house rule that copy never says certified. Added: claim commands run with your privileges; exit codes; dirty-tree note; running without install; `receipts-dir`. No em dashes. |
| 29 | `CLAUDE.md` | stamped | Rule 1 updated to the five-exemption reality and the two inert lines. |
| 30 | `LICENSE` | **not stamped, open** | Explicitly a placeholder pending the launch license decision. A placeholder cannot be certified; this is Krishna's call. |

## Open, needing a decision

- **LICENSE** is a placeholder by design. Until decided, the repository cannot be redistributed, and this file stays unstamped.
- **README's `uses: gaigenticai/countersign@v0.1`** assumes the GitHub org and a `v0.1` tag that do not exist yet; both are listed as open decisions in CLAUDE.md.

## Not changed on purpose

- The rule regexes themselves. They were tuned on Argus and the Argus gate still passes on its tree; changing them without that corpus would be guessing.
- `max_output_bytes` keeps its name (renaming a config key breaks every existing config); its documentation now says characters.
