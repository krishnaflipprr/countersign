<!-- audited on 20260903 -->
# God Audit: Countersign, 2026-09-03

Serial, one file at a time, bottom-up dependency order, every line read.
A file is stamped `audited on 20260903` only after it is 100%: no stubs, no
placeholders, no false statements, real data flow end to end, and it runs.
Where a file was not 100%, the code was fixed to green first and the fix is
recorded here. Tests were written before every behaviour change to a check.

## Standing verification (after the audit)

| Check | Result |
| --- | --- |
| `python3 -m unittest discover -s tests -t .` | 61 tests, OK (was 27) |
| `python3 -m countersign verify` | COUNTERSIGNED, exit 0, 11 files, 5 exemptions used, 2 inert markers noted |
| `python3 -m countersign verify --config demo/countersign.toml` | NOT COUNTERSIGNED, exit 1, 4 findings, 1 failed claim (as designed) |
| `python3 -m countersign check` | chain intact |
| `pip wheel .` then install into a clean venv | `countersign 0.1.0` boots; `init` + `verify` on a fresh tree countersigns |
| Evidence pack HTML (pass and fail variants) | rendered in a headless browser, both correct |
| Argus `scripts/check-no-stubs.sh` (source of the rules) | exit 0 on its 562-file tree, so the zero-false-positive statement still holds |

## Ledger

| # | File | Status | What was found and what was done |
| --- | --- | --- | --- |
| 1 | `.gitignore` | stamped (no stamp, not a comment-bearing file) | Correct; `.countersign/` ignored so receipts never land in git. |
| 2 | `pyproject.toml` | stamped | Added the 3.14 classifier (the suite runs on 3.14). Wheel builds and installs. |
| 3 | `countersign/__init__.py` | stamped | Version matches pyproject. |
| 4 | `countersign/__main__.py` | stamped | Correct. |
| 5 | `countersign/config.py` | stamped | **Fixed:** a scan path that does not exist was silently skipped, so a typo in `paths` produced a scan of nothing that then passed. Now a `ConfigError`. **Fixed:** a path outside the root crashed with a traceback; now a `ConfigError`. **Fixed:** malformed TOML and wrong-typed values (string where a list is needed, `timeout_s = 0`, booleans as integers) raised raw Python errors; all are now `ConfigError` with the reason. `claims.file = ""` now cleanly disables claims. |
| 6 | `countersign/stubscan.py` | stamped | **Fixed (false positives):** methods of a generic `Protocol[T]` were reported as empty bodies; a UTF-8 BOM made a valid file "unparseable". **Fixed (wrong line numbers):** `str.splitlines()` splits on form feeds and Unicode separators that Python's tokenizer does not, so every finding after a form feed was off by one and the empty-body exemption check read the wrong line. Lines are now split on `\r\n`, `\r`, `\n` only. **Fixed (crash on 3.11):** a null byte raises `ValueError` on 3.11 (SyntaxError on 3.12+); now caught either way. **Fixed:** dead decorator names `overridable` and `abc.override` (the latter could never match) removed. **Fixed (wrong count):** exemptions were counted for every line carrying the marker, including the marker's own definition, and a def-line marker was counted twice. Now only a marker that suppressed a finding counts as used; markers that suppress nothing are reported separately as inert. Six of the eleven rule-table markers were inert (a regex with alternation does not match its own source line) and were removed; the five that matter stay and are pinned by a claim. |
| 7 | `countersign/register.py` | stamped | **Fixed (chain could be broken by a race):** two runs on one checkout could read the same head and both append the same index, leaving a register broken forever and indistinguishable from tampering. Appends now hold an exclusive lock (`register.jsonl.lock`, flock on POSIX, msvcrt on Windows) across read-head and write. Verified with 48 concurrent appends from 8 threads. **Fixed:** binary garbage in the file raised `UnicodeDecodeError` instead of a verdict; a last line that parsed as JSON but was not an entry (a bare number) escaped `RegisterDamaged`. **Fixed:** constructing a `Register` created the receipts directory even for read-only commands. |
| 8 | `countersign/claims.py` | stamped | **Fixed (crash):** a command that printed anything and then timed out crashed the whole run (`TypeError: can't concat str to bytes`, because `TimeoutExpired` carries bytes even in text mode). **Fixed (crash):** any command output that was not valid UTF-8 raised `UnicodeDecodeError`. **Fixed (leak):** a timed-out command's children survived the kill; commands now run in their own process group and the whole tree is killed. **Fixed:** malformed claims TOML, `[claim]` written as a table instead of `[[claim]]`, and non-integer or zero `timeout_s` raised raw errors; all are `ClaimsError` now. Removed `run_claims`, which became unused. |
| 9 | `countersign/engine.py` | stamped | **Fixed (docstring was false):** it said the run is recorded before checks run; in fact scan and claims ran first and nothing reached the register until the end, so a killed run left no trace. `run_started` is now on disk before any check runs, findings and claim verdicts are appended as they happen, and a run that dies appends `run_aborted` on the way out. **Fixed (misleading receipt):** receipts named a commit with no indication the working tree differed from it. Receipts now record `git_dirty` and print "(uncommitted changes in the working tree)". `git not available` and `not a git repository` are now distinguished. **Fixed:** `--no-claims` skipped claims with no note on the receipt. **Fixed:** a missing config file crashed with `FileNotFoundError`; now a `ConfigError`. A bad config or claims file now refuses the run before anything is written. |
| 10 | `countersign/receipt.py` | stamped | **Fixed:** evidence containing a backtick (TypeScript template literals) or a multi-line command broke the Markdown tables; code spans now use double-backtick delimiters and flatten whitespace. Receipt JSON gains `git_dirty` and `scan.tests_excluded`. Skipped claims are now labelled "skipped, not passed". |
| 11 | `countersign/pack.py` | stamped | **Fixed (false statement):** the "not covered" list said test files were excluded even when `exclude_tests = false`; the bullet is now conditional. **Fixed (overstatement):** "every check above is deterministic" covered claim commands, which are the team's own; wording now separates the deterministic scan from claims run as declared. **Fixed (overstatement):** the footer said "no data left the machine", which Countersign cannot promise about claim commands; it now says Countersign itself made no network requests. Full SHA-256 shown instead of a truncated one with a literal "..." after "none declared". Added the `unparseable` row to the method table. |
| 12 | `countersign/reproduce.py` | stamped | **Fixed:** a hand-edited or truncated receipt crashed reproduce with `KeyError` or `JSONDecodeError`; every malformed shape is now a "not reproduced" verdict with the reason. **Fixed:** a claims file that no longer parses crashed instead of being reported. Removed `_claim_key`, dead code. Added a note when the number of scanned files differs from the run. |
| 13 | `countersign/cli.py` | stamped | **Fixed:** `check` caught `RegisterDamaged`, which `verify_chain` never raises, and nothing caught `OSError`. Config and claims errors now exit 2 (usage) instead of 1. Interrupt exits 130, not 2 (interrupt is not a usage error). The `init` template now matches the defaults (`.hg`, `.svn`) and says `max_output_bytes` is in characters, which is what the code measures. The register is verified once and reused rather than opened twice. |
| 14 | `countersign.toml` | stamped | Aligned `ignore_dirs` with the defaults. |
| 15 | `claims.toml` | stamped | The self-scan claim now also pins the used-exemption count at five. Added `register-chain-intact` (`countersign check`). |
| 16 | `tests/__init__.py` | stamped | Correct. |
| 17 | `tests/tamper_check.py` | stamped | Correct; fail-safe if the register format changes (an ineffective edit leaves the chain intact and the script exits 1). |
| 18 | `tests/test_config.py` | new, stamped | Covers every ConfigError above and the collect_files filters. |
| 19 | `tests/test_stubscan.py` | stamped | Added form feed, generic Protocol, abstractmethod, BOM, null byte, single-count and inert-marker tests. |
| 20 | `tests/test_register.py` | stamped | Added concurrency, binary garbage, and read-does-not-create-directory tests. |
| 21 | `tests/test_claims.py` | stamped | Added output-before-timeout, undecodable output, process-tree kill, malformed TOML, table-vs-array and bad-timeout tests. |
| 22 | `tests/test_gate.py` | stamped | Added run-start-before-claims (the claim command reads the register), missing config, claims-off note, conditional pack bullet, backtick escaping, git dirty state (real git repo), inert marker note, reproduce file-count note, damaged receipt tests. |
| 23 | `demo/countersign.toml` | stamped | Correct. |
| 24 | `demo/claims.toml` | stamped | **Fixed (wrong disproof command):** `wc -l \| grep -qx 0` can never match on macOS because `wc` pads its output, so the claim failed even on a clean tree. Now `grep -rqE` with `expect = "nonzero exit"`, portable. |
| 25 | `demo/app.py` | stamped | The planted defects are exactly the four the README promises; the file is a fixture and is meant to fail its gate. |
| 26 | `demo/test_app.py` | stamped | Correct. |
| 27 | `action.yml` | stamped | **Fixed:** `pip install` of the action path fetched setuptools from PyPI at run time, contradicting "zero dependencies"; the action now runs the checkout directly via `PYTHONPATH`. Inputs are passed through `env` rather than interpolated into the shell. Added `receipts-dir` so the artifact upload follows a moved receipts directory. Python pinned to 3.12 (3.11 is the floor, not the target). Branding icon `verified` is not a valid GitHub action icon; now `check-circle`. |
| 28 | `README.md` | stamped | **Fixed (impossible example):** the sample output showed an `empty-body` finding on a `.ts` file; that check is Python only. **Fixed (broken example):** the `pricing-is-live` claim piped through `grep -q`, which prints nothing, so `output contains` could never match. Sample output now matches the real terminal format (full commit, milliseconds, the policy note). Removed "documented" from the false-completion sentence (no source was cited) and "bank-grade" from the roadmap (nothing here has been through a bank). "production-certified" replaced by "reviewed file by file", per the house rule that copy never says certified. Added: claim commands run with your privileges; exit codes; dirty-tree note; running without install; `receipts-dir`. No em dashes. |
| 29 | `CLAUDE.md` | stamped | Rule 1 updated to the five-exemption reality and the two inert lines. |
| 30 | `LICENSE` | **not stamped, open** | Explicitly a placeholder pending the launch license decision. A placeholder cannot be certified; this is Krishna's call. |

## Open, needing a decision

- **LICENSE** is a placeholder by design. Until decided, the repository cannot be redistributed, and this file stays unstamped.
- **README's `uses: gaigenticai/countersign@v0.1`** assumes the GitHub org and a `v0.1` tag that do not exist yet; both are listed as open decisions in CLAUDE.md.

## Not changed on purpose

- The rule regexes themselves. They were tuned on Argus and the Argus gate still passes on its tree; changing them without that corpus would be guessing.
- `max_output_bytes` keeps its name (renaming a config key breaks every existing config); its documentation now says characters.

## Second pass, same day: license, TypeScript structural check, claims governance, user guide

Same rules. Every new file was written to the standard above and stamped on landing; every existing file it touched was re-read in full and re-stamped.

| # | File | Status | What was done |
| --- | --- | --- | --- |
| 31 | `LICENSE` | verbatim Apache-2.0, **not stamped** (license text must stay verbatim; downloaded from apache.org) | Decision taken: Apache License 2.0 for the whole repository. |
| 32 | `NOTICE` | new, not stamped (a NOTICE file is read by packaging tools and carries no comment syntax) | Copyright line and license pointer. |
| 33 | `pyproject.toml` | re-stamped | Package renamed `countersign-cli` (the bare name is held by an abandoned 2020 project). SPDX license, `license-files`, setuptools 77 floor. Wheel builds as `countersign_cli-0.1.0`. |
| 34 | `countersign/jsscan.py` | new, stamped | Structural empty-body check for TypeScript and JavaScript: blanks comments, strings, template literals (with `${}` nesting) and regex literals while keeping offsets, then finds function declarations, class and object methods, and exported arrow or function expressions whose block is empty. Constructors, Angular lifecycle hooks, control-flow keywords, unexported callbacks, exported noops, `.d.ts` and minified files are exempt. Run over 712 real TypeScript and JavaScript files (Argus and gaigenticOS, outside `node_modules`): zero hits on project code; the 39 hits were all inside a `.venv` copy of Playwright and each is a genuinely empty base-class method. The gate itself caught the first draft's comment containing the word "stub"; reworded. |
| 35 | `countersign/stubscan.py` | re-stamped | Dispatches the structural check by suffix; `.d.ts` and `.min.` files excluded from it. |
| 36 | `countersign/claimsdiff.py` | new, stamped | Claims compared against a git revision. Removed claim, changed expectation or changed needle are weakenings; changed command is listed for review. A base that cannot be read (no repository, bad ref, git missing) is a `ClaimsError`, never "no claims at base". A base file that exists but does not parse is reported and treated as empty. |
| 37 | `countersign/starter.py` | new, stamped | Starter claims for `init`, proposed only from build files that exist: package.json scripts (npm, pnpm, yarn or bun by lockfile), tsconfig with TypeScript, pytest or unittest, ruff, mypy, go.mod, Cargo.toml, Gemfile with spec/. Nothing guessed. |
| 38 | `countersign/config.py` | re-stamped | `[claims] required` (ids that must be declared) and `fail_on_weakened` (default true). |
| 39 | `countersign/claims.py` | re-stamped | `MISSING` status for a required claim nobody declared; `parse_claims` on text so the base revision can be parsed without a file. |
| 40 | `countersign/engine.py` | re-stamped | Required claims recorded as missing and failing; claims diff recorded in the register and failing on weakening when configured; `claims_diff` register entry. **Fixed:** an initialised repository with no commits was reported as "not a git repository"; now "no commits yet", dirty. |
| 41 | `countersign/receipt.py`, `countersign/pack.py` | re-stamped | `MISSING` rendered; claims diff section and counts in JSON, Markdown, terminal and pack. |
| 42 | `countersign/cli.py` | re-stamped | `verify --claims-base REF`; `claims diff --base REF` (exit 1 on weakening when the config fails on it); `init` now writes a starter `claims.toml` (kept if present) and sets `required` from what it found. |
| 43 | `action.yml` | re-stamped | On pull requests, fetches the base branch and passes `--claims-base origin/<base>`; `claims-base` input overrides. |
| 44 | `tests/test_jsscan.py`, `tests/test_claimsdiff.py`, `tests/test_init.py` | new, stamped | 40 fixture cases for the structural check (15 flagged, 25 not), diff logic and git-backed diffs including a subdirectory claims file and an unparseable base, and `init` end to end including a verify on the generated config. |
| 45 | `tests/test_gate.py` | re-stamped | Missing claims (with and without a claims file), weakened claim against a real git base failing then recorded, base without claims file rejected, repository with no commits. |
| 46 | `README.md`, `CLAUDE.md`, `countersign/pack.py` copy | re-stamped | Apache license section; what the register proves, exactly, and that owner-proof tamper evidence needs an external anchor; "Who guards the claims" section; TypeScript structural check described honestly. |
| 47 | `docs/guide.md` | new, stamped | User guide, every command with a screenshot produced by that command. |
| 48 | `docs/images/*.png` | new, binary (cannot carry a stamp) | Nine captures: init, verify failing, verify passing, claims diff, verify with a weakened claim, check, reproduce, and both evidence packs. Terminal captures are the real command output rendered in a terminal-styled page and photographed; nothing was retouched. |

Standing verification after the second pass: 90 tests OK; `countersign verify` COUNTERSIGNED (14 files, 5 exemptions used, 2 inert noted); demo NOT COUNTERSIGNED as designed; chain intact; `claims diff --base HEAD` clean; wheel builds and installs.

## Open, needing a decision

- **README's `uses: gaigenticai/countersign@v0.1`** assumes a `v0.1` tag that does not exist yet; tag it when the repository goes public.

## Third pass, same day: end-to-end test against real repositories

Ran the installed wheel (clean venv, Python 3.12) over clean `git archive` checkouts of Argus (715 in-scope files), gaigenticOS (1233) and CardOS (306), all three God-Audited, and compared with Argus's original `check-no-stubs.sh` on the same tree.

| Finding | Status | What was done |
| --- | --- | --- |
| Port was looser than the original on one rule: `NotImplementedError` matched `except NotImplementedError:` (a false positive in Argus's signal handling). The original requires `raise`. | fixed, test first | Rule is now `(raise|throw) [new] NotImplemented(Error|Exception)`. Its marker on the rule table became inert and was removed; four exemptions are pinned by the claim. |
| Structural empty-body check (which the original gate does not have) produced 21 hits on the three repos, every one a documented no-op: Alembic downgrades with a comment saying why, `close()` with a docstring saying there is nothing to close, a click group whose body is its docstring, a deliberately no-op migration. | fixed, test first | The check now reports a body that does nothing **and explains nothing**: bare `pass`, `...` or `{}`. A docstring or comment makes it a decision. The exemption marker alone is not an explanation. Zero empty-body hits on all three repos after the change; the demo's bare `...` is still caught. |
| Scope: the original gate covers `src/`, `dashboard/src/`, `bridge/`, `clients/`, `website/src/` only. With that scope the port reports exactly the original's four hand-allowlisted lines plus two "coming soon" lines (a rule the original lacks; the component is a coming-soon gate, so those are true by definition). With `paths = ["."]` it also reports seed-script prose ("sample data"), a demo webhook containing `XXX`, and comments about stubs. | documented | README, guide and the init template now say to point `paths` at production source. The rule words stay as ported; changing a tuned rule on one domain's vocabulary would be guessing. gaigenticOS's data-profiling code uses "sample values" as a domain term (8 lines) and would exempt them on the line. |
| The gate caught the audit's own prose three times (docstrings containing "stub" and "TODO"), each time correctly. | fixed | Reworded. |
| `.github/workflows/countersign.yml` | new, stamped | Runs the suite on Python 3.11, 3.12, 3.13 and 3.14 (3.11 is not installed locally; this is its first real run), builds the wheel, and runs the action itself on this repository. |
| GitHub Actions run of the workflow | **blocked, not a defect** | Run 33712296177 failed at startup with zero jobs. The run page states: "The job was not started because recent account payments have failed or your spending limit needs to be increased." Both workflow files pass actionlint. The action's shell steps were exercised locally (verify with a summary file, exit codes, claims base handling); the GitHub-specific parts (step summary rendering, artifact upload, PR base fetch, Python 3.11) remain unverified until the gaigenticai account's Actions billing is fixed. |

## Fourth pass: real GitHub runs (krishnaflipprr mirror, gaigenticai Actions locked by a stale 2025 billing block)

| Finding | Status | What was done |
| --- | --- | --- |
| CI on the action repository: unit tests on Python 3.11, 3.12, 3.13, 3.14, wheel build, and the action verifying its own repository. | green | First real 3.11 run; all green. |
| Cross-repo consumer (`countersign-e2e`, workflow written by `countersign init`, `uses: <owner>/countersign@v0.1`): run started, marker scan clean, `npm test` claim passed, COUNTERSIGNED. | green | Cross-repo use of a private action within one account works with `access_level = user`. |
| **Receipts artifact was empty**: upload-artifact v4.4+ skips hidden (dot-prefixed) paths by default and the receipts live in `.countersign`. The step said "No files were found with the provided path" and succeeded because `if-no-files-found: ignore`. | fixed | `include-hidden-files: true`. Released as 0.1.1; `v0.1` moved. |
| Runner warned that checkout@v4, setup-python@v5 and upload-artifact@v4 target deprecated Node 20. | fixed | Moved to checkout@v5, setup-python@v6, upload-artifact@v5 in the action, the generated workflow and our CI. |
| Pull request that weakens a claim (`expect` from `exit 0` to `nonzero exit`) on the consumer repo. | green | The action fetched the base branch, diffed the claims, printed "WEAKENED changed tests-pass", failed the gate, and the PR shows the failing check. Making that check required is branch protection on the customer's side. |
| Step summary on a real run. | green | Verdict table, claims table and notes render in the job summary as designed. |
| The runner reported the current majors of checkout, setup-python and upload-artifact as v7; the action used older ones with a Node 20 deprecation warning. | fixed | Moved to v7 across the action, the generated workflow and CI. Released as 0.1.2. |
| Actions budget on the account running the tests. | verified | krishnaflipprr has an Actions budget of $0 with stop usage on, so nothing can be charged; runs come out of the 2000 free private-repo minutes. |

## Hosting decision: krishnaflipprr/countersign is the product repository

Customers' generated workflows and the README now point at `krishnaflipprr/countersign@v0.1`. Released as 0.1.3 so every artifact that names the repository names the live one. The gaigenticai copies were deleted by Krishna on 2026-09-03.

## Release path

| Item | Status | Notes |
| --- | --- | --- |
| `.github/workflows/release.yml` | new, stamped, actionlint clean | Tag push `v0.1.N`: tests, gate, tag equals version, sdist and wheel, twine check, publish via PyPI trusted publisher in the `pypi` environment. No API token anywhere. |
| GitHub environment `pypi` | created | Reviewer requirement can be added later. |
| PyPI trusted publisher for `countersign-cli` | **blocked on Krishna**: PyPI requires two-factor authentication on the account before a publisher can be added. | Values to enter: owner `krishnaflipprr`, repository `countersign`, workflow `release.yml`, environment `pypi`. |

## Fifth pass: full re-read before the hosted build

Every file changed or added since the first stamp was re-read line by line (14 files were unchanged since their first certification and stand on it). Findings, all fixed with a test first:

| File | Finding | Fix |
| --- | --- | --- |
| `countersign/jsscan.py` | A `{}` **type** could be taken for a body: `read(): {};` in an interface, `(): {} \| null { ... }` as a return type. Both would have been reported as empty functions. | The type skipper now decides by what follows the block's `}`: another `{` is the body; `\|`, `&`, `[]`, `=>` continue the type; `;`, `,`, `)`, `>`, `=` end a signature with no body. |
| `countersign/jsscan.py` | `export const h = cond ? () => {} : real;` reported the exported binding as an empty function. | The arrow rule now accepts only the shape `[: type] = [async] [<T>] (params)\|ident [: type] => {`; anything else in the initialiser is not this rule's business. |
| `countersign/jsscan.py` | An `if/pass/else` construct and a condition of the form `c in ";\n" and c == ";"`. | Rewritten plainly. |
| `countersign/claims.py` | Claims bytes were decoded with replacement, so an undecodable byte inside a command would have been silently rewritten before running. | Strict UTF-8 (with BOM tolerated); undecodable is a `ClaimsError`. |
| `countersign/claimsdiff.py` | Whether the claims file existed at the base revision was decided by matching git's English error text, which git localises. | `git ls-tree` answers by exit code and output; no message parsing. Dead `REVIEW_FIELDS` removed; an `assert` used for control flow replaced. |
| `countersign/starter.py` | `bun test` runs bun's own test runner, not the package's `test` script. | `bun run test` for bun. |
| `countersign/receipt.py` | Markdown code spans used a fixed double-backtick fence; evidence containing ``x`` (reStructuredText in Python docstrings) would break the table. | Fence is one backtick longer than the longest run inside, per CommonMark. |
| `action.yml` | With `fail-on: warn`, a usage error (exit 2) was announced as "NOT COUNTERSIGNED". | Exit 1 is the verdict; any other non-zero is "could not run". |
| `README.md` | Status line still said "structural Python check" and `init` "writes countersign.toml and a starter claims.toml". | Both updated to what the code does. |

Verification after the pass: 102 tests OK; gate COUNTERSIGNED; demo NOT COUNTERSIGNED; Argus 715 files / gaigenticOS 1233 / CardOS 309 re-scanned with zero structural findings and the same marker findings as before; both workflows actionlint-clean.

## 2026-09-05: step 1 of the hosted plan, in the open CLI

| File | Status | Notes |
| --- | --- | --- |
| `countersign/plain.py` | new, stamped | The receipt in plain words: one sentence per fact, derived only from the run's recorded facts; used at the top of the pack and the Markdown summary. The words for each marker rule live on the rule's own definition line in `stubscan.py` (the gate caught the first draft naming a rule id in a separate table). |
| `countersign/reportclaims.py` | new, stamped | Claims proposed from an agent's completion message by deterministic English patterns; commands derived only from the repository (test runner, build script, file path, URL); unresolved sentences reported with the reason, never guessed. `countersign claims from-report FILE|- [--write]`. |
| `action.yml` | re-stamped | Opt-in `attest` input using GitHub Artifact Attestations, off by default: free for public repositories on every plan, private repositories need Enterprise Cloud (verified against GitHub's documentation). |
| `tests/test_plain.py`, `tests/test_reportclaims.py` | new, stamped | 13 tests. |
| Version | 0.2.0 in code, **not tagged** | Tagging publishes to PyPI, which makes the source public; Krishna decides when. |
